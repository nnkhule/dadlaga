@page "/"

@inject NavigationManager Nav

@code {

    protected override void OnInitialized()
    {
        // NavigateTo must be called during SSR with forceLoad:false and replace:true
        // so Blazor's router does it as a client-side navigation, not a server-side redirect loop.
        Nav.NavigateTo("/login", replace: true);
    }

}
